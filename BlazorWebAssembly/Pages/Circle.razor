@page "/circle"
@page "/c"

<div class="circles">
    <!--input type="string" @bind="@title" /!-->
    @for (int i = 0; i < strings.Count; i++)
    //@for (int i = 0; i < strings.Count; i++)
    {
        var id = i;
        <div class="circle">
            <button class="circleButton" @onclick="(() => newText(id))">@strings[id]</button !-->
        </div>

}
</div>

<style>
    .circleButton {
        border-radius: 400px;
        height: 200px;
        width: 200px;
        color: black;
        fill: blue;
        background-color: #2fb7d0;
    }
    .circles {
        display: grid;
        grid-template-columns: repeat(auto-fill, 12rem);
        grid-gap: 0.5rem;
        justify-content: center;
        padding-left: 0;
        padding-bottom: 5px;
    }

</style>

@code {
    string color { get; set; } = "#2fb7d0";

    string title { get; set; } = "Untitled";

    List<string> strings = new List<string> {"no", "silver", "bullet", "a", "b", "c", "d", "e"};

    private void newText(int id) {
        strings[id] = title;
    }
   
}

