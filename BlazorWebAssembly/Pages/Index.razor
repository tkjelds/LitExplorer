@page "/"
@inject HttpClient Http
@inject NavigationManager NavigationManager
<PageTitle>Index</PageTitle>
<h1>Hello, world!</h1>
<Circle></Circle>
<button class="btn btn-success" @onclick="Create">Add new</button>

@code {
    private void Create()
    {
        var uri = $"{NavigationManager.BaseUri}addNewNodeDebug";

        NavigationManager.NavigateTo(uri);
    } 
}